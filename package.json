{
  "name": "api-automation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {"start:api": "json-server --watch db.json --port 3000",
              "test": "playwright test",
              "test:api": "npm run start:api & npx wait-on http://localhost:3000 && npm run test"},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.3.0"
  }
}
